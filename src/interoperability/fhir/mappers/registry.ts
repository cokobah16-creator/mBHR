// Moved to resources/registry.ts (one module per resource type); kept so
// existing imports keep working.

export { RESOURCE_DEFINITIONS, PUBLISHED_TYPES, isPublishedType } from "../resources/registry";
export type { ResourceDefinition } from "../resources/module";
